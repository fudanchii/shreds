<div class="sidebar-nav-fixed well">
  <%= render 'subscribe' %>
<div class="nav-container">
  <ul class="nav nav-list">
    <% @categories.each do |cat| %>
      <li class="nav-header"><%= cat.name.camelize %></li>
      <ul class="nav nav-list">
        <%= render 'feeds_title', feeds: cat.feeds %>
      </ul>
    <% end %>
  </ul>
</div>
</div>
